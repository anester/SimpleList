@{int cur = 0; }
@foreach (SimpleListData.UserList item in Model)
{
    if (cur == 3)
    {
        cur = 0;
        @:<div class="row"></div>
	    }

        @Html.Partial("ListPart", item)

    cur++;
}